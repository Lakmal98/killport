{
  "name": "killport",
  "version": "0.4.0",
  "private": true,
  "description": "CLI utility to terminate processes bound to ports",
  "scripts": {
    "test": "bash tests/run.sh",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
